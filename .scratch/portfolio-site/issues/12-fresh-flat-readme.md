# 12 — Fresh-Flat README rewrite

**What to build:** Fresh-Flat's README reads as a public-facing project description instead of linking to Otago-only SharePoint docs, so anyone (recruiter or otherwise) landing on the repo understands the project.

**Blocked by:** None — can start immediately (separate repo, content-only)

**Status:** resolved

- [x] README rewritten to describe the project, its stack, and how to run it, without relying on links only accessible to Otago students
- [x] Newton's specific contribution (flat/invite CRUD API, join/leave-flat features) is called out
- [x] Live demo link included

## Comments

Already done prior to this check — `Fresh_Flat/FreshFlat`'s README was rewritten in commit `8733af1` ("Rewrite README as a public-facing project description"). Verified against all three acceptance criteria directly; no further changes needed.
